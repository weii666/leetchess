# Implementation Plan

> **範圍原則**:列表是導航,不是第二個對局介面。`web/` 既有八個模組的邏輯一律不動 —— 本 spec 只碰檔名、一個返回連結,以及一個不碰引擎池的索引端點。

## 1. 索引:讓列表有資料可讀

- [x] 1.1 新增題目索引端點
  - 在 `service/main.py` 新增 `GET /api/catalog`,回傳題庫全部題目的列表所需欄位(題號、局名、描述、難度、標籤、出處、可解標記)
  - **不得借引擎、不得觸發搜尋** —— 它只讀啟動時已建好的題庫索引。引擎池滿時此端點仍須正常回應
  - 新增題目後重啟服務即出現在回應中,不需修改程式
  - 完成狀態:引擎池關閉的情況下該端點仍回 200 且內容完整;回應含題庫中每一題,欄位齊備
  - _Requirements: 5.1, 5.2, 5.3, 5.4_
  - _Boundary: service/main.py_

## 2. 純資料層

- [x] 2.1 (P) 實作題目索引的取得與篩選
  - `web/catalog.js`:取得索引一次,之後的篩選全在記憶體中進行,**不因篩選再次取得資料**
  - 依難度、標籤、出處篩選;多條件為 AND
  - **過濾掉可解標記為 false 的題目**;**可解標記為空值時視為可上架** —— corpus-verification 尚未回填,若把空值當不可解會讓整個題庫消失
  - 純資料,不碰 DOM
  - 完成狀態:單條件、多條件 AND、無結果三種情形各自正確;可解標記為 false 的被排除而為空值的仍列出
  - _Depends: 1.1_
  - _Requirements: 1.2, 1.3, 1.4, 2.1, 2.2, 2.3, 2.4, 5.1, 5.2_
  - _Boundary: web/catalog.js_

- [x] 2.2 (P) 實作完成狀態的讀寫
  - `web/progress.js`:完成狀態的唯一寫入處,存於瀏覽器本機,**鍵名帶版本前綴**使日後格式變更能辨識舊資料
  - **解析失敗一律退回「全部未完成」** —— 使用者手動改壞、舊版本殘留、瀏覽器塞了別的東西,任何一種都不該讓列表打不開
  - **不再列出的題號保留在集合中** —— 集合是題號的集合,不是列表的鏡像;題目下架再上架時使用者的標記還在
  - **不自動寫入任何題號** —— 只有使用者的切換操作會改變它
  - 完成狀態:寫入後可讀回;餵入損毀的儲存值時退回全部未完成而非拋錯;不再列出的題號不致失敗
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.6, 3.7_
  - _Boundary: web/progress.js_

## 3. 列表頁

- [x] 3.1 建立列表頁骨架並讓出入口
  - **將既有的 `web/index.html`(對局頁)改名為 `web/play.html`**。**內容一字不動** —— 題號本來就由 `?id=` 從外部傳入
  - **新建的列表頁就叫 `web/index.html`**,不叫 `list.html`
  - 骨架含:題目列表容器、完成計數、空狀態與錯誤提示區
  - **不放篩選區** —— 篩選互動已移入 Backlog,先擺一個不能用的空殼比沒有更糟
  - 既有測試中引用 `index.html` 之處一併更新為 `play.html`。**不可全域取代** —— 純資料層測試(`test_web_catalog.py`、`test_web_progress.py`)提到的頁面只是承載 `page.evaluate()` 的空殼,與對局頁無關,改錯會讓它們悄悄開始依賴一個不存在的頁面
  - 完成狀態:開啟服務的入口位址(`/`)看到的是列表頁骨架而非棋盤;`/play.html?id=1` 仍是可用的對局介面;既有測試零退化
  - _Depends: 1.1_
  - _Requirements: 1.1, 6.2_
  - _Boundary: web/index.html, web/play.html, tests/_

  > **為什麼列表頁叫 `index.html` 而不是 `list.html`**:`service/main.py:461` 以 `StaticFiles(html=True)` 掛在 `/`,根路徑是靠**目錄下的 `index.html`** 解析的。若新頁面叫 `list.html`,`/` 會直接 404,得回頭改 `main.py` 加一條根路由 —— 那既在本任務 boundary 外,也讓入口多一層沒必要的間接。沿用 `index.html` 則 `/` 自動就是列表頁,`service/` 一個字都不用動。
  >
  > 注意 `web/list.js` 與 `web/list.css`(任務 3.2)**維持原名**:模組名不必跟著頁面名走,而 `index.js` 這種名字反而說不出它是什麼。

- [x] 3.2 呈現列表與完成標記
  - `web/list.js`:把索引畫成列表,每列顯示**題號、局名、難度、標籤**四項;題號與局名為主要識別
  - **出處與描述不在列表呈現**,改由對局介面顯示(任務 4.2)—— 列表是掃視用的,每列擠進越多欄位越難掃。索引端點仍照常回傳這兩個欄位,前端拿了不畫而已
  - 每題一個可切換的完成標記,切換後立即反映;完成題數與總題數可見
  - 題庫為空時告知使用者而非呈現空白畫面
  - 全文繁體中文
  - `web/list.css`:**桌面尺寸下讀得順的基本版面**,與既有 `style.css` 共用視覺語言。**已完成與未完成須一眼可辨** —— 這是可用性底線,不是裝飾。行動裝置適配見 Backlog
  - 完成狀態:瀏覽器中每一列可見題號、局名、難度、標籤四項,且**不出現出處與描述**;點擊完成標記後該題狀態改變且計數更新;**重新載入頁面後標記仍在**;已完成的列與未完成的列在畫面上明顯不同
  - _Depends: 2.1, 2.2, 3.1_
  - _Requirements: 1.2, 1.3, 1.5, 3.1, 3.2, 3.3, 3.5, 6.2_
  - _Boundary: web/list.js, web/list.css_

## 4. 往返

- [x] 4.1 自列表進入對局
  - 點選列表中的一題即開啟該題的對局介面,題號經由既有的 `?id=` 契約交接
  - 完成狀態:自列表點第一題後,對局介面載入的**確實是那一題**(以題目資訊比對,不是只看有沒有開啟)
  - _Depends: 3.2_
  - _Requirements: 4.1, 4.2_
  - _Boundary: web/list.js_

- [x] 4.2 自對局返回列表,並在對局介面顯示描述
  - 在對局介面加入返回列表的途徑。**這是本 spec 唯一改動 `web/app.js` 的地方,其餘邏輯一字不動**
  - 返回後完成標記仍在
  - **顯示該題描述** —— 描述已自列表移來(1.2 / 4.5)。**出處本來就已呈現**(web-play-runtime 的 requirement 1.2),不必再做;`GET /api/positions/{id}` 的回應**早已含 `source` 與 `description` 兩個欄位**,後端不必動,也**不要為了拿描述去打 `/api/catalog`**
  - (本行原先把出處與描述寫反了。實作時的 RED 階段證否:出處測試原封不動通過、描述測試回 `None`)
  - 出處與返回途徑一起做,是因為兩者都只改 `web/app.js` 一個檔;拆成兩個任務會讓同一個檔被動兩次
  - 完成狀態:自對局返回列表後,先前的完成標記仍在;對局介面可見該題的出處與描述
  - _Depends: 4.1_
  - _Requirements: 4.3, 4.4, 4.5_
  - _Boundary: web/app.js, web/list.js_

## 5. 驗收

- [ ] 5.2 端到端:自列表選題、下完、返回
  - 對真實服務走一遍完整動線:開啟入口 → 看到列表 → 標記一題完成 → 點進該題 → 走一手 → 返回列表 → 確認標記還在
  - **不使用攔截**,打的是真實後端與真實引擎
  - 完成狀態:整條動線在瀏覽器中走得通;返回列表後完成標記保留
  - _Depends: 4.2_
  - _Requirements: 1.1, 3.3, 4.1, 4.2, 4.3, 4.4_

---

## 範圍說明

本任務計畫涵蓋 requirements.md 的 Requirement 1、3、4、5,以及 Requirement 6 的 6.2。**Requirement 2(篩選)與 6.1 / 6.3(行動裝置版面)已移入下方 Backlog。**

## Backlog(本輪不做,已列出以備日後接續)

以下兩項的**純資料層與後端支撐都已完成**,缺的只是介面與樣式。日後接續時不需回頭改既有模組。

- **篩選互動(原 3.3,Requirement 2)** —— 難度、標籤、出處三種篩選,多條件取交集;生效中的條件須對使用者可見;無結果時告知並提供一鍵清除。
  - **`web/catalog.js` 的 `filterPositions()` 已實作並經 14 個突變驗證**,AND 語意、空條件、無結果三種情形皆已覆蓋。接上介面即可,不需重寫資料層。
  - 由於本輪無呼叫者,`filterPositions` 會暫時只被測試使用。**刻意保留,不刪** —— 它是這一項的全部難處所在。
  - 觸發條件:題庫規模成長到光靠捲動找不到題目為止。**目前題庫只有 1 題,篩選沒有意義。**
  - **⚠ 一併要處理 requirement 4.4**:返回列表走的是 `<a href>` 整頁導覽,列表的記憶體狀態全丟。4.4 要求「保留返回前的篩選條件」,今天因篩選未實作而空洞成立,**篩選一落地就立刻破功**。屆時需要讓條件走 URL query 或 sessionStorage —— 現行導覽方式沒為它留任何餘地。

- **列表上的出處(自 1.2 移出)** —— 出處已改由對局介面顯示,列表只留題號、局名、難度、標籤。
  - **觸發條件:題庫收錄第二本書。** 屆時同名或相似的排局在列表上分不出是哪一本的,而題號雖然唯一卻看不出書名。目前只有《適情雅趣》一本,不成問題。
  - 屆時不必回頭改資料層:`/api/catalog` 與 `catalog.js` 一直都帶著 `source` 欄位,只是列表沒畫。

- **行動裝置適配(原 5.1 的一半,6.1 / 6.3)** —— 行動裝置直向畫面完整呈現列表且**不需橫向捲動**。
  - **原 5.1 已拆開**:桌面基本版面與「已完成/未完成可辨」併入 3.2(那是可用性底線,不是裝飾),Backlog 只留行動裝置適配。`web/list.css` 因此在 3.2 就會建立。
  - 既有 `web/style.css` 的行動裝置處理(web-play-runtime 5.1)可直接借鑑,**尤其那一輪的教訓:被當成 load-bearing 而寫進註解的 `min-width: 0` 經突變證實完全無作用,真正生效的是 `width: 100%`。** 抄樣式時連同註解一起抄會把錯誤的因果一併帶過來。

**索引以端點提供而非靜態檔**:後端啟動時已掃描 `positions/` 建好索引,端點只是把它讀出來,不碰引擎池。靜態檔要靠產出工具維護,題庫變更後忘記重跑就會過期,而過期的列表會讓使用者點進一個不存在的題目。

## Implementation Notes

- 沿用既有的前端測試手法:Playwright 的 Python 綁定、`page.route()` 合成 origin 供**真實交付檔**、純資料層以 `page.evaluate()` 驗證。
- **測試的沉澱信號是等 `#waiting` 轉隱藏**,不要數 `#moves li`(web-play-runtime 4.4 的教訓)。
- **點擊座標要依實際 bounding box 縮放**,不可直接用 viewBox 使用者座標(web-play-runtime 5.1 的教訓)。
- 1.1:`GET /api/catalog` 回 `{"positions": [...]}`,欄位為 `id`、`title`、`description`、`difficulty`、`tags`、`source`、`solvable`。**刻意不含 `fen` 與 `state`** —— 那兩樣需要引擎,加回來就會悄悄破壞 5.2。
- 1.1:**端點不注入 `GameService`,只依賴 `Repository`** —— 借引擎的程式碼根本不存在。review 以「啟動後立刻關閉引擎池」的真實服務驗證:catalog 回 200 三題完整,同一服務的 `/api/positions/1` 回 503。
- 1.1:**parent 授權的邊界擴張**:`PositionRepository` 新增 `all()`(純新增、唯讀、依題號遞增)。原因是它原本沒有列舉 API,而以 `get()` 從 1 逐一探測**會在題號有缺口時靜默截斷** —— review 以突變證實:題庫 1/2/201 會掉成 1/2,直接違反 5.3。
- 1.1:端點**不過濾** `solvable === false`,依 design 把 1.4 的過濾留給 `catalog.js`。**空值與 false 在前端語意不同**:空值視為可上架,否則 corpus-verification 跑完之前整個題庫是空的。
- 1.1:`CatalogEntry` / `CatalogResponse` 定義在 `main.py` 而非 `models.py`,是 boundary 所致。日後若放寬可一併搬遷。
- 2.1:`web/catalog.js` 匯出 `loadCatalog()`(取索引一次,回傳凍結的 `{positions, filter}`)、`filterPositions()`、`isListable()`。**`isListable` 就是 `position?.solvable !== false`** —— 只有明確的 `false` 排除,`null` 與欄位不存在都留著。寫反會讓 corpus-verification 跑完前整個題庫空掉。
- 2.1:**認不得的回應形狀一律失敗,不降級成空陣列** —— 「題庫真的沒題目」(1.5 的空狀態)與「索引壞掉」(錯誤提示與重試)在呈現上是兩件事,合併會讓後者看起來像前者。
- 2.1:難度條件經 `Number()` 轉換以吸收下拉選單的字串值;認不得的值轉成 `NaN` 而**篩不到任何題目**,不默默當成未選。`difficulty: 0` 正確仍可篩(無 falsy 錯誤)。
- 2.1:逾時與「不洩漏後端原文」的手法與 `web/api.js` **重複一份**(design 把 `catalog.js` 的依賴列為「無」)。日後若抽共用層,這是兩處會一起改的地方。
- 2.1:**待改進**(非阻斷):逾時測試以「掛住」而非乾淨失敗偵測退化 —— `page.evaluate()` 在 Playwright 同步 API 下無預設逾時。加 `page.set_default_timeout()` 可讓退化轉紅而非卡住。
- 2.2:`web/progress.js` 匯出 `loadCompleted()`(不收參數 —— **結構上就沒有拿列表修剪集合的機會**,3.7 因此不靠自律)與 `toggleCompleted(completed, id)`。鍵為 `leetchess:v1:completed`,值為數值遞增的題號陣列。
- 2.2:**雙參數形狀是 design 定位的結果**,不是權宜:design 把本模組定為「純函式 + localStorage、依賴為無」,模組級可變狀態與那個定位相衝。狀態由 `list.js` 持有,3.2 接手時用 `completed = toggleCompleted(completed, id)`。順帶擋掉的壞結果:每次從儲存區重推導,會在儲存不可用時讓標記**按得上去卻永遠取消不掉**(review 以突變實測四種模式全中)。
- 2.2:**寫入失敗不告知使用者、也不把切換退回去。** 注意這是**類推**而非引用 —— design 的 Error Handling 與 3.6 講的都是**讀取端**的損毀,寫入端無明文。類推的依據是 3.2 的「立即反映」:拋錯或退回切換會直接牴觸。已知代價:**隱私模式下使用者會以為進度存下來了,其實沒有。** 日後若要告知,加一個 `storageAvailable()` 之類的獨立匯出即可,**不需更動 `toggleCompleted` 的簽章**。
- 2.2:**「解析失敗」的範圍比字面廣得多** —— 取用 `localStorage` 這個動作本身就可能拋(隱私模式、儲存被擋)、API 根本不存在、`getItem`/`setItem` 各自拋錯、配額爆掉。測試以 `Object.defineProperty` 在 **import 之前**造出四種真實情境,不是把例外塞進 try 就算數。
- 2.2:陣列混雜物**逐項丟棄**而非整份退回(`[1, "oops", 6]` → `{1, 6}`)。3.6 的後件是失敗時的退路,不是「有機會就清空」的授權;整份丟棄會因一筆髒資料抹掉使用者其餘標記。
- 2.2:**review 抓到的洞值得記著** —— 37 個測試裡沒有一條讓集合真的歸零(原本那條取消標記的測試刻意留了第二個題號)。在 `writeIds` 開頭加一行 `if (completed.size === 0) return;` 這種常見的「空集合就不寫」最佳化,37 條全過,而使用者取消唯一標記後重開**標記會復活**。補測試後該突變只讓新測試轉紅。**測試數量從來不是覆蓋的證據。**
- 3.1:對局頁改名為 `web/play.html`(git 記為 `R100`,`shasum` 前後皆 `ac278cd6…`,內容一字未動),新的 `web/index.html` 為列表頁骨架。容器:`#positions`、`#progress`(內含 `#completed-count` / `#total-count`)、`#empty`、`#error`(內含 `#retry`)。**`service/` 零改動** —— 這正是沿用 `index.html` 之名的全部目的。
- 3.1:**`#retry` 按鈕是 design 授權的前瞻,不是實作者自創** —— design 的 Error Handling 明列「索引取不到 → 重試」。它落在預設 `hidden` 的 `#error` 內、無事件處理器,3.2 之前使用者看不到死控制項。(實作者原本的理由「3.2 回頭改不到 index.html」**不成立**,`list.js` 大可動態建出按鈕;結論對而理由錯,記正確的那個。)
- 3.1:**改名的連帶影響靠逐一判讀,不可全域取代**。10 個提到 `index.html` 的測試檔分三類:真測對局頁的三個(`test_web_page` / `test_web_play` / `test_web_e2e`)改路由;六個只改註解 —— 它們的 `/index.html` 是 `route.fulfill(body=...)` 的**行內字串空殼**,在 `WEB_DIR` 查找之前就攔下,真實檔案根本碰不到;純資料層兩個(`test_web_catalog` / `test_web_progress`)一字未改。review 以在四個模組各注入 `throw new Error` 反證那些測試確實還在測東西(分別 25/25/37/22 條轉紅)。
- 3.1:`test_web_play.py` 保留 `("index.html" if path == "/" ...)` 那一行 —— 它就是 `StaticFiles(html=True)` 的行為,合成來源照著它走才不會與真實服務漂移。
- 3.1:**待 3.2 補**(非阻斷):`#error` 的 `role="alert"` **無測試覆蓋** —— 拿掉它 41 條測試全過。可及性語意屬 3.2 的呈現範疇,屆時補一條 `get_attribute("role") == "alert"`。
- 3.1:**3.2 要注意**:`test_the_list_page_ships_no_filter_controls` 以 `"<input" not in text` 釘住「不放篩選區」,讀的是**靜態檔字串**。完成標記若用 `<input type="checkbox">` 且由 `list.js` 動態產生,不受影響、**不必改那條測試**。
- 3.1:改名被另一條平行工作的 docs commit(`f5400f2`)一併掃入,`git log --follow` 追改名史時會落在一個 `docs(...)` 訊息底下。
- 3.2:`web/list.js` 是唯一碰 DOM 的模組。列的結構契約為 `li.position[data-id][data-completed]` + `input.position-toggle`。**4.1 接導航時沿用 `data-id`,不要另起一套。**
- 3.2:**完成題數取「完成集合 ∩ 目前列出的題目」而非集合大小** —— 集合裡留著下架或不在索引中的題號(3.7),直接數會出現「已完成 5 / 3 題」。
- 3.2:**載入中與失敗時計數顯示 `—`**(沿用 `web/app.js:112` 的既有慣例),因為那時「總共幾題」還不知道,**寫 0 等於宣稱題庫是空的**。真的空題庫才是 `0 / 0`,兩者分得開。requirements 3.5 對這兩種過渡狀態無明文,屬類推。
- 3.2:切換後整份重畫並**把焦點放回同一題的核取方塊** —— 否則鍵盤使用者每標一題就被丟回頁面開頭。這是「整份重畫」這個選擇的唯一補償,已有測試釘住(走真實的 Space 鍵路徑)。
- 3.2:**`borderLeftColor` 在 `border-style: none` 或寬度為 0 時照樣回報顏色值。** review 據此刪掉整條 `border-left`,金邊在畫面上完全消失而 44 條測試全綠 —— 6.3 是本任務唯一標為「可用性底線」的條目,卻押在一條守不住的斷言上。**測 CSS 的可見性必須連 `borderLeftStyle` 與 `borderLeftWidth` 一起驗。**
- 3.2:`list.css` **複製了一份 `style.css` 的調色盤**(design 把 `list.css` 的依賴列為「無」,`index.html` 只掛 `list.css`)。原先號稱有測試防漂移,review 實測**只覆蓋 body 的 3 個屬性,另外 8 個 token 漂移全部存活**,其中包括 `--accent` —— 完成標記的強調色與計數顏色,最可能被單獨調整的那一個。現已改為比對兩頁 `:root` 上 `SHARED_CUSTOM_PROPERTIES` 全部十個屬性,並斷言不得解析為空字串(否則「兩邊都掉了」會冒充「一致」)。**新增共用變數時要一併加進那份清單。**
- 3.2:出處/描述的外洩檢查改讀 `innerHTML` 而非 `inner_text()` —— 後者**對 `display: none` 的內容是盲的**,文字仍在 DOM 裡、Ctrl+F 找得到。
- 3.2:夾具含一題 `difficulty: 0`。`service/positions.py` 的 `_read_int` 對難度**沒有下界**,0 收得進來;2.1 正是在同一個 falsy 陷阱上被抓過。
- 3.2:**`web/list.js:236` 模組層那次 `render()` 是死碼** —— `start()` 在同一個同步回合就把 `loading` 設為 true 並重畫。留著當防禦性 no-op。
- 3.2:失敗時 `positions = []` 的清空目前**使用者到不了**(`#retry` 在成功後即隱藏的 `#error` 內,走不出「成功 → 再失敗」)。4.x 若讓列表可重新載入就會浮現。
- 4.1:入口是**局名上的真 `<a href="./play.html?id=<題號>">`**,不是整列覆蓋式連結、也不是 JS 跳轉。因此「按完成標記不會跳走」是**結構的結果**(核取方塊落在 `<a>` 之外,review 以 DOM 探針實查 `boxInsideAnchor: false`),不靠 `stopPropagation` 或 z-index 疊放。中鍵開新分頁、右鍵複製網址、Enter 鍵全部免費附帶。
- 4.1:**熱區只有局名一欄**,難度與標籤欄不可點 —— 與 requirements 明列的參照形態(grind75、leetcode problemset)一致,兩者都把連結收在題名欄。覆蓋式連結的代價是文字無法選取、連結失去可讀文字內容(得靠 aria-label 撐),且上述結構保證會退化成疊放順序的保證。
- 4.1:**題號取自題目自身的 `id`,絕不用列的位置。** 測試以**有缺口的題號**(1 / 2 / 201)釘住 —— 在今天連號的題庫上,「以索引推題號」完全看不出問題,收第二本書之後每一列都會指錯題。
- 4.1:「載入的確實是那一題」必須**合成對局頁的回應**才驗得到 —— 題庫只有 1 題時,點哪一列都看到同一題。`route_api()` 讓 `/api/catalog` 與 `/api/positions/{id}` 由同一個處理器接手(**Playwright 後註冊的路由優先,分開註冊 `/api/**` 只有最後一條生效**),並記錄題目端點實際被要的題號作為第二層證據。
- 4.1:`align-self: stretch` 讓點擊區域是整格而非幾個字寬,**有測試釘住其實質**(標籤折行的夾具:有 stretch 48.4px、無 28.8px)。關鍵是夾具先斷言 `content > 40` 證明「這一列真的因標籤而長高」—— 3.2 那條假金邊斷言缺的正是這一層前置證明。
- 4.1:**連結的原樣位址也要驗,不能只驗解析後的結果。** 服務今天掛在網域根目錄,`/play.html` 與 `./play.html` 解析結果一模一樣,「改用絕對路徑」這種退化完全看不出來;一旦部署到子路徑底下,絕對路徑會跳出整個應用。review 發現此洞後已補斷言,並以突變確認轉紅。
- 4.1:`encodeURIComponent(id)` 是**死防禦** —— 題號一路是整數,今天構造不出需要轉義的值。留著方向正確,但別在註解裡宣稱它有作用。
- 4.1:`test_marking_a_position_complete_does_not_open_it` 用 **300ms 固定等待**。「證明一件事沒發生」在導航語境下只能靠時間窗;其失效模式是**假通過**(慢機器上導航超過 300ms),**不會造成假紅**,故不污染 CI。
- 4.1:`.position-title:focus-visible` 與 `.position-toggle:focus-visible`(3.2 留下的)**都無測試覆蓋**。焦點環經實測確實生效,屬既有的一致缺口。
- 4.2:**任務描述原本把出處與描述寫反了。** `#puzzle-source` 從 web-play-runtime 起就在 `play.html` 與 `app.js` 裡(源自其 requirement 1.2),而 `description` 在 `web/` 底下一個字都沒有。RED 階段直接證否:三條出處測試原封不動通過、三條描述測試回 `None`。**本輪真正新增的是描述。** requirements 4.5 與本任務描述皆已更正。
- 4.2:`web/app.js` 的改動是**純加法**(+76/−1)。那個 −1 是 `renderPuzzleInfo` 的解構加上 `description`,無任何既有簽章或流程更動。返回連結與描述節點都由 `app.js` 動態建出,`play.html` 與 `style.css` 的結構未動。
- 4.2:**返回連結在模組載入時就掛上,不等題目載入成功** —— 題目載入失敗的畫面上,另一個控制項「重來」對不存在的題目只會無限重試,沒有出路。review 以「改成載入成功後才掛」的突變確認只讓 `test_the_way_back_survives_a_position_that_fails_to_load` 一條轉紅。
- 4.2:**「返回後標記仍在」是真的往返**,不是只讀一次儲存區。review 以「對局頁 `localStorage.removeItem(進度鍵)`」的突變確認它只讓 `..._round_trip_through_a_game` 一條轉紅,與 `..._survives_a_reload` 完全隔離。
- 4.2:描述取自 `GET /api/positions/{id}` 已有的回應,**不為了一個字串多打一次 `/api/catalog`** —— 那還會讓列表與對局頁對同一題的資料可能不一致。
- 4.2:**parent 授權的邊界擴張**:`web/style.css` 新增 `.back-to-list` 三條規則(屬 web-play-runtime 的檔)。不上樣式的話它吃瀏覽器預設的藍紫底線,是這個橘色調介面裡**唯一的藍紫**,看起來像功能沒接完。有測試釘住:比對的是「顏色與側欄其餘說明文字相同」而非某個色碼字面值,底線另外驗。
- 4.2:**那條樣式測試第一次寫錯了,值得記著** —— 少了導覽完成的等待,`evaluate` 在舊頁面上跑、`getElementById` 回 `null`,測試以 `TypeError` 收場。**它看起來是紅燈,其實什麼都沒驗到**,而且施加任何突變都「轉紅」,一度看似證明有效。判準是看錯誤訊息是不是自己寫的那句斷言。
- 4.2:**⚠ requirement 4.4 的「保留篩選條件」目前是空頭支票。** 返回走 `<a href>` 整頁導覽,列表的記憶體狀態全丟。今天篩選未實作(Requirement 2 在 Backlog)所以無從觀察,但**篩選一落地這一半就立刻破功**,而現行導覽方式(無 URL query、無 sessionStorage)沒為它留任何餘地。已記入 Backlog 的篩選項。
- 4.2:行動裝置上返回連結位於側欄最前(`prepend`),實測 360×640 / 390×844 / 414×896 三個尺寸下 top 分別為 394 / 427 / 454px,**全部落在第一屏內,不需捲過棋盤**。
- 4.2:`tests/test_web_layout.py` 的 `SIDEBAR_SELECTORS` 未含 `#back-to-list` / `#puzzle-description`(該檔屬 web-play-runtime)。review 以臨時探針把該檔全套邊界盒斷言套到兩者、跑遍行動與桌面尺寸全綠,故目前無遮蔽。
- 4.2:**真實題庫的 `description` 與 `title` 內容近乎重複**(局名「適情雅趣 第 21 局」/ 描述「《適情雅趣》第 21 局」),側欄因此有一行幾乎沒有資訊量。屬 position-corpus 的資料形態問題。
